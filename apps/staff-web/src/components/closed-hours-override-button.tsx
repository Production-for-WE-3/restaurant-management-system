export function ClosedHoursOverrideButton({
  closed,
  label,
  onConfirm,
  disabled,
}: {
  closed: boolean
  label: string
  onConfirm: () => Promise<void>
  disabled?: boolean
}) {
  void closed
  void label
  void onConfirm
  void disabled
  return null
}
